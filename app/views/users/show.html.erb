<script>
  var user_id = parseInt('<%= current_user.id %>'),
      facebook_id = parseInt('<%= current_user.uid %>');
  var FacebookCard = FacebookCard || {};
  FacebookCard.drawCard(user_id, facebook_id);
</script>